# Project Lessons

Project-specific lessons, incidents, and reusable observations.

Do not use this as a daily changelog.
Only record lessons that may change future behavior.

## Entry format

## YYYY-MM-DD - Short title

- source_type: project-incident | ai-self-correction | user-feedback | test-failure
- task_type:
- domain:
- trigger:
- incident_or_feedback:
- lesson:
- scope: project | project-type | global-candidate
- landed_in:
- next_action:

## 2026-06-17 - JSON.stringify into a <script> block is XSS unless you escape "<"

- source_type: ai-self-correction / security
- task_type: implementation / security-review
- domain: server-rendered EJS views inlining data as <script type="application/json"><%- JSON.stringify(x) %></script>
- trigger: QA fuzzing entered a destination name `</script><script>alert(1)</script>`; it executed on /workbench/inland.
- incident_or_feedback: `JSON.stringify` does NOT escape `</script>`, `<script>`, `<!--`, or the JS line separators U+2028/U+2029. Any user-editable string inlined verbatim into a `<script>` block via `<%- %>` (unescaped EJS) can close the tag early and inject executable markup — a stored XSS. The repo had 15 such emissions across 5 workbench/calculator views (names, concepts, remarks, proveedor/cliente all flow in). The generated PDF was safe because it uses `<%= %>` (HTML-escaped).
- lesson: never inline `<%- JSON.stringify(x) %>` directly into a `<script>` block. Use a shared `safeJson()` that does `JSON.stringify(x).replace(/[<  ]/g, c => "\\u"+c.charCodeAt(0).toString(16).padStart(4,"0"))` — escaping `<` (blocks `</script>`/`<script>`/`<!--`) and the line separators produces valid JSON that parses byte-identically but can never break out of the element. Expose it via `res.locals` so every template can use it. Add a stored-XSS smoke guard (enter `</script>` in a name, assert no raw breakout + JSON still parses).
- scope: global-candidate (applies to any server-rendered app inlining JSON in <script>)
- landed_in: QA batch B1 — res.locals.safeJson + all 15 emissions; smoke-test stored-XSS guard.
- next_action: any new `<%- JSON.stringify` into a <script> must use safeJson; keep the smoke guard.

## 2026-06-17 - Two normalizers for the same shape must stay in lockstep (parse vs store)

- source_type: ai-self-correction
- task_type: code-review / implementation
- domain: quote header + line-item normalization (server parse path vs store persist path)
- trigger: a deep re-read of batch1/2 found server.js parseQuoteHeader was updated to the new option sets (INLAND, new cargo types, transportMode) but store.js normalizeQuoteHeader was NOT — so a quote saved as a draft and re-read silently reset INLAND→OCEAN, new cargo→FCL, and dropped transportMode. The same gap existed in normalizeQuoteLineItem (missing section/unitOfMeasure/conceptEs).
- incident_or_feedback: when a value object flows through TWO normalizers — the request parser (server parseX, used for the live action) and the persistence normalizer (store normalizeX, used on load/save of drafts) — adding a field or widening an enum in one without the other creates a "works live, lost on round-trip" bug that no single-shot test catches. The PDF generated immediately looked correct; only save-draft→reload lost data.
- lesson: when you add/normalize a field on a request-parse path, grep for the matching store normalizer (`normalize<Same>`) and update both in the same change; add a draft/persist round-trip test (normalizeShippingData on a built object) asserting the new field survives AND that old objects get a back-compat default. Treat parse-vs-store as a pair.
- scope: project-type
- landed_in: batch3 P0 (store normalizeQuoteHeader) + S3 (normalizeQuoteLineItem section/uom/conceptEs); scripts/r2-batch3-test.js round-trips.
- next_action: for any quote/inland field change, update parseX + normalizeX together and add a normalize round-trip assertion.

## 2026-06-17 - Large autonomous multi-feature batch: commit-per-item + test-after-each + scope honestly

- source_type: ai-self-correction
- task_type: implementation / planning
- domain: Jose R2 batch1+batch2 (23 H/O/Q items across handover, customs, inland, quote)
- trigger: a single "do it all autonomously, questions at the end" mandate spanning two large batches.
- incident_or_feedback: the safe way to run a very large autonomous batch is one focused commit per H/O/Q item, each with a smoke/quote/targeted test run BEFORE committing, so a failure is localized and nothing is lost if context summarizes mid-run. Data-model changes (box_53, fixedCharges basis/required/amount, nameZh/nameEs, origins, unitOfMeasure/section, remark library) each went through the store normalizer with back-compat defaults + a focused round-trip test. A few items had genuine modeling ambiguity (per-point price override; remark drag-reorder; ES line-item concept) — these were implemented to a solid functional first pass and the scoped-down part was named explicitly rather than faked.
- lesson: for a big autonomous run — (a) keep a TodoWrite list and commit per item so progress survives a compact; (b) run the existing test (smoke/quote) after every item and extend it with a round-trip assertion for each user-facing fix (empty-cell-creates-rate, RCL add-tramo, add-port/delete-port, origin empty-shell, 4-language PDF); (c) when a sub-feature needs a product decision you can't make, ship the unambiguous 90% and state the deferred 10% in the commit + final report — don't block the whole run or guess silently; (d) when a smoke assertion encodes OLD behavior you intentionally changed (e.g. quote-settings bounce → real admin page; svg→png logo), update the test in the same commit.
- scope: project-type
- landed_in: PRs #6 (batch1) and the batch2 PR; commits r2-1a..r2-2b.
- next_action: reuse the commit-per-item + test-after-each rhythm for large batches; always name scoped-down sub-pieces explicitly.

## 2026-06-17 - Stacked-PR rebase-merge conflict recovery + re-verify stale plan premises against merged HEAD

- source_type: ai-self-correction
- task_type: deployment / planning
- domain: git PR merge workflow; spec-first investigation
- trigger: merging a clean linear stacked pair (PR#3 batch1→main, PR#4 batch2→batch1) then writing the next batch's spec.
- incident_or_feedback: (1) Rebase-merging the lower PR (#3) gave batch1 a NEW sha on main (fa6a7e0 ≠ 8088125); retargeting the upper PR (#4) to main then went CONFLICTING because files touched by BOTH batches 3-way-conflicted against the rebased base. (2) The execution plan's file:line refs and bug premises were written pre-batch2-merge and were partly stale: O6.7 VEHICLE_LABEL_KEYS was already fixed by batch2; "admin-module.ejs shared by handover+customs" was false (renderAdminRules sends customs→admin-customs.ejs, handover→admin-module.ejs); H4 add-set already seeds a first tramo; customs fixedCharges cells were already always-editable.
- lesson: (a) For a stacked pair, either merge-commit both (preserves SHAs, deterministic) or merge the lower PR then recover the upper WITHOUT force-push by cherry-picking only its incremental commits onto a fresh branch off the new main (verify `git diff --stat <orig-tip> HEAD` is EMPTY = byte-identical), close the conflicted PR, open a replacement. Don't rebase-merge a stacked lower PR if you then need the upper PR to retarget cleanly. (b) When the plan was authored before recent merges landed, re-verify every file:line and every "X is broken" premise against the actual merged HEAD before specifying changes — several "bugs" may already be fixed and several "shared" files may not be shared; report the corrections instead of coding to the stale premise.
- scope: project-type
- landed_in: docs/specs/20260616_batch1_fixes_SPEC.md (C1-C4 corrections); merge recovery via PR#5.
- next_action: prefer merge-commit for stacked pairs, or cherry-pick-onto-fresh-branch recovery; always diff the recovered branch against the original tip to prove content equality; open each batch spec by re-confirming line refs against HEAD.

## 2026-05-05 - Avoid Per-Option Admin Release Buttons

- source_type: ai-self-correction
- task_type: implementation
- domain: customs admin UI
- trigger: adding release controls for occupied shipping-line / container-type storage assignments.
- incident_or_feedback: the first implementation rendered one release button per occupied option and expanded the admin HTML payload to about 22 MB.
- lesson: for dense assignment matrices, avoid duplicating large option lists into secondary controls; render one compact control per card and derive its choices client-side from existing disabled options.
- scope: project
- landed_in: views/admin-customs.ejs, public/app.js
- next_action: when changing large admin matrix UIs, include a quick rendered HTML size or DOM-volume check before finalizing.

## 2026-05-06 - Persisted Defaults Need Migration

- source_type: user-feedback
- task_type: implementation
- domain: customs storage rules
- trigger: user reported that production still showed the old third storage tier after the default was changed to two tiers.
- incident_or_feedback: changing seed/default builders did not remove legacy persisted DB/runtime JSON state.
- lesson: when changing default rule structure, add a versioned normalization or migration path for existing persisted data, not just new drafts and repository seed JSON.
- scope: project
- landed_in: src/lib/store.js
- next_action: for future data-shape defaults, test against a fixture that simulates the old persisted shape.

## 2026-05-06 - Shared Mobile Chrome Must Check Admin In Spanish

- source_type: ai-self-correction
- task_type: implementation
- domain: workbench/admin responsive UI
- trigger: compacting the shared sidebar/header for the handover workbench.
- incident_or_feedback: browser regression found the Spanish `/admin/handover/settings` page had horizontal overflow because admin cards and tables could force a wider grid item.
- lesson: shared mobile chrome changes need admin-page checks in both supported languages, and grid/card wrappers should use `min-width: 0` so table overflow stays inside table cards instead of widening the page.
- scope: project
- landed_in: public/styles.css
- next_action: include Spanish mobile admin overflow checks when changing shared sidebar, header, cards, or grid wrappers.

## 2026-06-10 - Inland module: OSRM, link resolution, seed-data diff hygiene

- source_type: ai-self-correction
- lesson: (1) OSRM public routing needs a base-URL fallback chain + retries + serial spacing; `steps=true` gives ferry detection via `step.mode === "ferry"`. (2) Mexican tariff CSVs are often semicolon-delimited because `,` is the thousands separator — detect the delimiter from the header instead of hardcoding. (3) Google Maps link resolution must whitelist domains and follow short links manually (capped redirects, no cookies) to avoid SSRF; coordinate priority is `!3d!4d` > `@` > `q/ll` > `/dir` > bare text. (4) When `saveShippingData` runs full normalization, re-saving the seed JSON re-applies other modules' (idempotent) migrations and pollutes the diff — inject only the changed module section back into the HEAD file to keep PR diffs scoped.
- scope: project
- landed_in: src/lib/inland-routes.js, src/lib/inland-csv.js, src/lib/inland-link-resolver.js, data/shipping-lines.json
- next_action: when seeding/editing data files via the store, verify `git diff` is scoped to the intended module; reuse the OSRM fallback + delimiter-detection patterns for future data imports.

## 2026-06-11 - Inland finalize: real tarifario seed, encoding, same-key tiers, ferry

- source_type: ai-self-correction
- lesson: (1) CSV encoding must be auto-detected from the file Buffer — strict UTF-8 first (TextDecoder fatal:true), fall back to Latin-1, strip BOM — because operator re-exports from Excel are UTF-8/CP1252 and a hardcoded latin1 read mojibakes accented rows (e.g. CIUDAD ACUÑA) and silently drops them. (2) An idempotent rate-merge key of (destinationId,proveedor,cliente,commodity) is insufficient: the real data has two LTP rows for the GDL/Zapopan corridor identical on every column except price (29,000/43,000 vs 43,000/66,000). Fix = a file-order dupIndex appended to both the merge key and the id hash (also fixed a latent bug where the two rows produced the same entry id); size-1 groups keep dupIndex=1 so existing identities are unchanged; idempotency holds because same file -> same order -> same dupIndex -> in-place update. (3) Verify *post-merge final counts*, not just pre-merge produced counts, against acceptance. (4) The public OSRM car profile does NOT take the Mazatlán→La Paz ferry — it routes ~3614 km all-land down Baja — so step-mode ferry detection alone misses it; a road/straight-line detour-ratio (>2.5x) flags it cleanly and isolates only La Paz.
- scope: project
- landed_in: src/lib/inland-csv.js, src/lib/inland-routes.js, src/lib/store.js, scripts/seed-inland-from-csv.js
- next_action: when importing operator CSVs, decode by buffer with UTF-8→latin1 fallback; when deduping rows, confirm the chosen identity key actually distinguishes real-world rows and validate final counts; validate routing-engine assumptions (ferry/profile) against the live engine before encoding them as acceptance.

## 2026-06-13 - Inland go-live: confirm deploy target, seed must replace, scope the seed-file diff

- source_type: project-incident
- task_type: deployment / data-migration
- domain: inland go-live (merge feature/inland-routes-map to main + production Supabase seed)
- trigger: merge to main, deploy to antropy-expressline-production, then put the 300 real rateEntries into the production DB while removing any demo data.
- incident_or_feedback: four go-live gotchas. (1) The Railway deploy source was NOT inferable from the repo: no deploy config (no railway.json/toml, Procfile, nixpacks, Dockerfile), docs describe GitHub→Railway only generically, and the local `railway` CLI was linked to a DIFFERENT project ("MXQ Dashboard" → web-production-fa73d) with `railway list` returning Unauthorized — so the target service was neither visible nor controllable from here; the deploy mechanism (auto-deploy on push to main) had to be user-confirmed. (2) The task premised "12 demo rateEntries in prod", but a read-only check of the prod DB showed `modules.inland` EMPTY (0 rateEntries) — the demo seed never deployed because inland was never merged; always read the target before trusting the premise. (3) `scripts/seed-inland-from-csv.js` did an idempotent MERGE only, so it would have left demo + real coexisting; added a `--replace` flag that swaps `inland.rateEntries` wholesale. (4) Re-running `inland:seed` to propagate morelos/edomex `needsReview=false` rewrote the WHOLE shipping-lines.json (6k-line diff) because `saveShippingData` re-normalizes every module (handover/customs containerType + storage-ruleset migrations the committed seed was behind on) — exactly the diff-pollution from the 2026-06-10 lesson.
- lesson: before any go-live write — (a) positively confirm which Railway project/service serves the target domain and how it deploys (a generated `*.up.railway.app` host is its own service; don't trust the linked CLI project), and confirm the local `.env` DATABASE_URL is the prod DB by fingerprinting a distinctive live value (here: the handover line list incl. the "WHAN HAI" typo matched the live `/workbench/handover`); (b) read the target's current state before overwriting — premises about "demo data to delete" can be wrong; (c) make the seed REPLACE (not merge) so placeholder and real data can never coexist; (d) propagate catalog/destination metadata via a seed-version bump (gated re-seed), and when only one module changed, surgically patch just that module's section in the committed JSON rather than re-saving through the full normalizer (which migrates and reorders every module).
- scope: project
- landed_in: src/lib/inland-catalog.js, src/lib/store.js (INLAND_SEED_VERSION 2 + coordSource whitelist), scripts/seed-inland-from-csv.js (--replace), data/shipping-lines.json (surgical inland patch)
- next_action: reuse the fingerprint-the-live-value technique to confirm a `.env` points at prod before writing; keep `--replace` as the go-live seed mode; when editing one module in a store-managed JSON, patch in place to keep the diff scoped.

## 2026-06-16 - Inland v2 batch2: 6-tier vehicle types — label-map + seed-shape parity

- source_type: ai-self-correction (audit-found, pre-commit)
- task_type: feature / data-model extension
- domain: inland vehicle types (sencillo/full + 4 new tiers), routing-provider abstraction, case photos
- trigger: extending serviceType from a 2-value (sencillo/full) to a 6-tier enum.
- incident_or_feedback: a pre-commit audit caught two latent defects from the binary→enum widening. (1) `computeInlandCalculator`'s explanation text still used `serviceType === "full" ? serviceFull : serviceSencillo`, so ALL four new tiers (1.5t/3.5t/8t/lowboy) would render as "Sencillo" in the human-readable formula — the value math was correct, only the label was wrong. (2) `buildInlandDestinationSeed()` did not emit the new `imageUrls` field that `normalizeInlandDestination` adds, so seed-shape and normalized-shape diverged. Also confirmed by-design: burreo (short-haul) only has CSV data for sencillo/full, so new tiers compute burreo=0 until José provides per-tier data.
- lesson: when widening a binary discriminator to an N-value enum, grep for EVERY `=== "<oldvalue>" ? ... : ...` ternary that branches on it (labels, formulas, sort keys, map-data), not just the price lookup — the math is the obvious site, the human-facing label is the easy miss. And when adding a normalized field, add it to the seed builder too so seed-shape == normalize-shape (otherwise the first re-seed silently changes the object shape). Verify replace_all actually hit all occurrences — identical strings at different indentation are distinct matches.
- scope: project (label-map technique is a cross-project candidate)
- landed_in: src/lib/calculate.js (VEHICLE_LABEL_KEYS + vehicleLabel), src/lib/store.js (buildInlandDestinationSeed imageUrls), src/lib/inland-vehicles.js (catalog)
- next_action: on the next enum widening, sweep all branch sites; keep seed builders in lockstep with normalizers.

## 2026-06-17 - Quote modes: a new quote-level discriminator must ride the full parse→store→default path, and the render layer was already data-driven

- source_type: project-feature
- task_type: feature / data-model extension
- domain: quote report modes (mexico_only vs ocean_mexico) + NO MEXICO preset rows
- trigger: adding a `quoteMode` quote-level field that decides whether the NO MEXICO (ocean + China-origin) section is preset, plus 12 foreign template rows.
- incident_or_feedback: two things that could have gone wrong but were handled by following existing patterns. (1) PARSE/STORE/DEFAULT parity (same class as the earlier P0 normalizeQuoteHeader↔parseQuoteHeader miss and the QA normalizer-parity lesson): a new quote field is invisible/lossy unless it is added in THREE places — the request shaper (`buildQuoteFormData`), the persistence normalizer (`normalizeQuoteDraft`), and the admin default preset (`normalizeQuoteHeaderDefaults` + the admin save). Missing any one means the value silently resets on draft round-trip or fresh load. (2) The PDF/workbench render was ALREADY section-driven: `groupRowsBySection` drops empty sections, so "mode one hides NO MEXICO" needed NO template branch — it falls out of producing zero foreign rows. Checking the render layer's existing data-driven behavior first avoided adding a redundant `if (mode...)` in the EJS. Back-compat came free: old drafts have no `quoteMode` → `normalizeQuoteMode` defaults to `mexico_only` == legacy (foreign rows didn't exist before), and old quotes have no `section:"foreign"` rows → render only the MEXICO section.
- lesson: (a) when adding a record-level discriminator, thread it through request-parse + store-normalize + default-preset in lockstep, and default it to the value that reproduces legacy behavior; (b) before branching a template on a new mode, check whether the render is already keyed off data presence (empty-group/empty-section dropping) — the mode may just be a data filter; (c) make the mode→rows reconcile idempotent (drop foreign when off; prepend the foreign block only when entirely absent) so recompute/round-trips don't duplicate or clobber operator edits; (d) reuse the controlled fee-code vocabulary for new preset rows (code autofills concept), and flag still-unconfirmed prices in the row remark (origin charges = CNY→USD pending José) rather than inventing an FX rate.
- scope: project (parse/store/default parity + "render already data-driven" are cross-project candidates; reinforces the existing normalizer-parity lesson)
- landed_in: src/lib/quote.js (foreign QUOTE_TEMPLATE_ROWS, QUOTE_GROUP_ORDER, QUOTE_MODES, normalizeQuoteMode, buildInitialLineItems(mode), reconcileLineItemsForMode), src/lib/store.js (normalizeQuoteDraft + headerDefaults quoteMode), src/server.js (buildQuoteFormData/assembleQuoteView/saveDraft/admin), views/workbench-quote.ejs + admin-quote.ejs + public/quote.js (mode selector, data-driven categories)
- next_action: next quote-level field → add to buildQuoteFormData + normalizeQuoteDraft + headerDefaults together; when José returns origin-port USD/FX, replace the placeholder prices and clear the pending-FX remark.

## 2026-06-20 - Mixed batch r3: font/rates/CONTENTO yards/new-carrier/metadata — data-vs-code prod propagation + parallel-list mirrors

- source_type: project-feature + ai-self-correction (investigation-found)
- task_type: mixed batch (UI density / rate data / yard seed / new feature / metadata)
- domain: quote line-item CSS, handover shipping-line rates+metadata, customs CONTENTO yards, new-carrier onboarding
- trigger: A compact quote editor; B KMTC rate+label edits; C CONTENTO Manzanillo yard seed (método B); D create/delete shipping line; E HAPAG code + 14 RFC.
- incident_or_feedback (the reusable findings):
  1. PROD DATA ≠ CODE PROPAGATION. Production runs Postgres/Supabase (DATABASE_URL set → `shouldUseDatabase()` true), so `getShippingData` reads `app_state`, NOT `data/shipping-lines.json`. A Railway deploy ships CODE (CSS, routes, normalizers) but does NOT run `db:seed`; the JSON DATA changes (B rates, C yards, E rfc values) therefore do NOT reach prod until someone runs `npm run db:seed` — which OVERWRITES the live app_state (any José admin edits). So a "data" round must distinguish "code that deploys on merge" from "seed data that needs an explicit, backed-up re-seed". Normalizer-added fields (E rfc default null, C contento seed) only take effect on the seed side; existing prod rows keep their stored values.
  2. PARALLEL LIST MIRRORS. `modules.customs.shippingLines` is a SEPARATE persisted list (lightweight `{id,name,active,notes,yardIds}`), NOT auto-derived from handover. The quote selector IS handover-derived (auto), but the customs yard↔line mapping reads the customs copy. So a new carrier must be written to BOTH lists (add), kept in sync on edit (name/notes), and cascaded on delete (mirror + yard.shippingLineIds). Always grep for sibling copies of an entity before assuming one write covers it.
  3. SAVE-VALIDATION SHARP EDGE. The handover line save aborts early (error flash, no persist) if ANY demurrage rule set's rules fail `applySequentialRuleUpdates`. A freshly-added rule set saves fine via its own add-route, but the NEXT full form save needs that set's rule day-fields (the rendered form supplies them; a hand-built test body that omits them silently loses the whole save incl. unrelated name/charge edits). When testing a save path, build the COMPLETE form body or do the unrelated-field save before adding sub-entities.
  4. SURGICAL LARGE-JSON EDIT. For a store-managed 1.9MB JSON, verify `JSON.stringify(JSON.parse(orig),null,2)` is byte-identical to the original (here: identical except a trailing newline the file lacks) BEFORE regenerating — then a script that parses, replaces ONE sub-tree, and re-stringifies produces a diff confined to that sub-tree. Key it by the taxonomy the normalizer uses (customs re-keys onto handover's 20 container types, not the stale 17 on disk), or the values get zeroed on load.
  5. MISSING SOURCE DATA. The CONTENTO PDF named in the prompt was absent from the repo. Did NOT fabricate 24 unknown maniobra prices — seeded the station scaffolding + the 2 confirmed prices + confirmed wash fees, set the rest to 0 with per-station "pendiente PDF" notes, kept shippingLineIds empty (método B) so the inert yards cannot mis-cost, and flagged the gap in the report + José question list.
- lesson: separate "code deploys on merge" from "seed data needs a backed-up db:seed" and say so explicitly in any data round; mirror/cascade every parallel copy of an entity; build complete form bodies when testing aborting save validators; prove JSON round-trip fidelity before regenerating and key data by the normalizer's live taxonomy; never invent prices for missing source data — scaffold + flag.
- scope: project (findings 1/2/3/4 are cross-project candidates → LESSON_CANDIDATES)
- landed_in: views/workbench-quote.ejs (A), data/shipping-lines.json (B/C/E), src/lib/contento-yards.js + store.js + scripts/seed-contento-yards.js (C), src/lib/store.js normalizeShippingLineNotes + scripts/seed-shipping-line-metadata.js (E), src/server.js + views/admin-module.ejs + src/lib/i18n.js + scripts/d-add-shipping-line-test.js (D)
- next_action: before claiming B/C/E are "live in prod", confirm prod storage + run a backed-up `db:seed` OR have José apply the 3 KMTC edits via the now-live admin UI; when José returns the MSC ruling + CONTENTO PDF + 7 carriers' rates, fill them.

## 2026-06-20 - r3 data deploy: prod has client edits → diagnose-then-surgical-patch, never blind db:seed

- source_type: project + ai-self-correction (investigation-found, prevented a clobber)
- task_type: data landing to production (Supabase) + audit
- domain: shipping-data app_state (handover rates, customs yards, carrier metadata)
- trigger: B/C/E JSON data changes had to reach Supabase prod (they don't deploy with code); plus filling 26 CONTENTO real prices.
- incident_or_feedback: before writing prod I backed up + diagnosed prod app_state vs the pre-change baseline and found PROD WAS NOT PRISTINE SEED — José had made admin-UI edits live: CMA-CGM doc fee 45→50, ZIM concept renames (Import Container/Borrar), COSCO rate edits, KMTC ISD already 15 (he applied it himself), and TWO operator-created yards ("新场站 4/5"). A blanket `npm run db:seed` (the obvious way to push JSON→Supabase) would have WIPED all of it. Instead wrote a surgical patch that changes ONLY the B/C/E fields (14 rfc + HAPAG/ONE code, KMTC renames, swap the 3 fake demo yards for the 26 CONTENTO while KEEPING José's 2 created yards) via saveAppState (no full re-normalize, so prod's exact shape + edits survive), dry-run first, re-backup before --apply. Also: the diff had to be key-order-insensitive (canonical sort) — a naive JSON.stringify compare flagged every line as "changed" purely from normalizer key-ordering, which would have hidden the few real edits in the noise.
- lesson: (a) production data on a managed store is NOT the seed file — assume the client has edited it; back up + diff prod-vs-baseline BEFORE any write and classify real edits vs normalizer noise (sort keys before comparing). (b) Prefer a surgical, dry-run-first patch that touches only the intended fields and writes raw (saveAppState) over a full re-seed/re-normalize that clobbers edits and churns unrelated fields. (c) Separate "code constants deploy live" (templates/types/normalizer fields — verified live in prod) from "instance data needs a migration" (rates/yards/metadata) when auditing whether past work actually landed. (d) Keep full prod snapshots as local rollback anchors (gitignored — they're live business data), record path+sha256 in the roadmap.
- scope: project (a/b/c are strong cross-project candidates → LESSON_CANDIDATES)
- landed_in: scripts/patch-prod-data.js, scripts/seed-new-carriers.js, src/lib/contento-yards.js (real prices), scripts/audit-contento-test.js, scripts/audit-new-carrier-test.js, docs/specs/20260620_data_deploy_audit_REPORT.md
- next_action: after Chandler approves, run patch --apply, spot-check prod (KMTC new names + ISD 15 + rfc; 26 CONTENTO + José yards present; José edits intact); when José sends the 7 carriers' rates, fill the shells.

## 2026-06-21 - r3 data deploy execution: a full-blob FX write storm clobbered data writes (found + fixed)

- source_type: ai-self-correction (prod incident found + fixed) + project
- task_type: landing data to production (Supabase) + concurrency bug fix
- domain: shipping-data app_state persistence, exchange-rate refresh
- trigger: applying the approved surgical patch (B/C/E + 7 shells) to prod.
- incident_or_feedback: the patch wrote successfully but was reverted within ~1 second, every time. Measuring the row showed prod was writing app_state shipping-data ~every 3s (revision 211k+), and a field-level diff showed each write changed ONLY exchangeRates while preserving the modules. Root cause: the FX refresh (per-request when stale + a daily scheduler + an external forced-refresh hitting prod ~every 3s) called saveShippingData, which overwrites the WHOLE shipping-data blob. Because the FX HTTP fetch is slow, each save was a full-payload overwrite based on a several-seconds-stale read — so it reverted ANY concurrent edit to carrier/customs/inland data. This was not just blocking my patch: José's own admin saves could be silently clobbered by the FX writer (a real data-integrity bug). Fix: db.patchAppStateField (jsonb_set a single top-level key) + store.saveExchangeRates; both FX save sites now update ONLY {exchangeRates} in DB mode, so FX can never touch module data. Validated jsonb_set is byte-identical-on-modules with a no-op against prod. Then upgraded the patch to compare-and-swap + verify-persist, deployed the fix (had to merge-before-patch, inverting the planned order), and the patch landed and stayed; 19/19 spot-check (B/C/E live + every José edit preserved).
- lesson: (a) a managed-store record that is a single large JSON blob is a concurrency trap — a frequent writer of ONE field (FX rates) that does read-modify-write of the whole blob will silently revert every other field's concurrent edits; persist hot/independent sub-fields with a targeted update (jsonb_set), not a full-blob rewrite. (b) When an external write "won't stick", measure the row's write rate and diff what each write changes before assuming your write failed — the clobberer revealed itself as a 3s exchangeRates-only churn. (c) Landing data against a live writer needs CAS + verify-persist, but CAS is not enough if the live writer full-overwrites — you must also stop it from overwriting your fields (here: make FX targeted). (d) Order can invert under a blocker: the data patch required deploying the persistence fix FIRST. (e) Always keep the pre-write backup — the first clobber made it momentarily look like data could be lost.
- scope: project (a/b/c are strong cross-project candidates → LESSON_CANDIDATES)
- landed_in: src/lib/db.js (patchAppStateField), src/lib/store.js (saveExchangeRates), src/server.js + src/lib/exchange-rate-scheduler.js (use it), scripts/patch-prod-data.js (CAS + verify-persist)
- next_action: investigate/stop the external ~3s forced FX refresh (now harmless but wasteful — ~28k DB writes/day); consider moving exchangeRates to its own app_state key entirely.

## 2026-06-21 - FX write storm: a forced-refresh endpoint hammered every 2s; throttle the work, not just the trigger

- source_type: ai-self-correction (prod incident, root-caused + fixed) + project
- task_type: production write-storm diagnosis + fix; full go-live audit
- domain: exchange-rate refresh, app_state write volume
- trigger: prod app_state shipping-data was being written ~every 2s (~47k/day).
- incident_or_feedback: the round-19 fix had made FX writes harmless (jsonb_set so they no longer clobbered module data) but did NOT stop the ~28-47k/day write volume. Root-causing it: each write changed ONLY exchangeRates.lastCheckedAt (rates stable, lastError null) = a SUCCESSFUL forced refresh; needsExchangeRateRefresh was FALSE (rates fresh for the day) so it could only be force:true; the sole force path is POST /admin/:moduleKey/exchange-rates/refresh — i.e. an authenticated external source POSTing it ~every 2s (no frontend poller exists in the code). Frankfurter publishes DAILY rates, so these forced re-fetches were pure churn. Two fixes: (1) THROTTLE the actual work — even a forced refresh is a no-op (no fetch, no write) if rates were checked within a window (default 15min); this caps a 2s-poller to ~96/day regardless of the trigger. (2) The /exchange-rates/refresh route ALSO did a redundant full-blob saveShippingData after loadShippingData already persisted via jsonb_set — removed it (redundant + a clobber risk under hammering; missed in the round-19 pass because that pass only fixed loadShippingData's internal save). Verified: 0 writes in 60s post-deploy (was 11/20s); rates still valid/current; throttle unit test 5/5.
- lesson: (a) when you can't stop an abusive caller (no access to the external source / request logs), throttle the EXPENSIVE WORK it triggers, not the trigger — an idempotency/min-interval gate caps the blast radius regardless of call frequency. (b) Diagnose a write storm by diffing what each write actually changes (here: only lastCheckedAt → a no-op refresh) and by checking the gate that should have prevented it (needsRefresh FALSE → so it must be force). (c) When you fix a frequent writer's persistence, grep for EVERY call site that writes the same blob — a route had its own redundant saveShippingData that the first fix missed. (d) For data that updates on a daily cadence (FX reference rates), a "refresh now" button must still be rate-limited or it becomes a write amplifier when polled.
- scope: project (a/b/c are cross-project candidates → LESSON_CANDIDATES)
- landed_in: src/lib/exchange-rates.js (throttle + export needsExchangeRateRefresh), src/server.js (drop redundant saveShippingData in the refresh route), scripts/audit-fx-throttle-test.js
- next_action: find + stop the external ~2s POST /exchange-rates/refresh source (Railway access log / an open admin tab / a cookie'd script) so it stops making HTTP requests at all; remove the dead admin-masters.ejs refresh-exchange-rates form (route doesn't exist).

## 2026-06-20 - RMW read storm: egress, not writes — an uncached full-blob read path bled ~70GB/day

- source_type: ai-self-correction (prior FX framing was incomplete) + project
- task_type: production egress-storm root-cause + structural read/write fix; thick regression
- domain: shipping-data app_state read/write path, in-process caching, Supabase egress quota
- trigger: Supabase free egress (5GB) blown ~70x (361.6GB/7d); pg_stat_statements showed 218k full-blob SELECTs ≈ 211k full-blob writes.
- incident_or_feedback: the prior rounds framed the Supabase blowout as the "FX WRITE storm" and the round-21 throttle did cap writes (probe confirmed 0 writes/min in prod now). But egress kept bleeding because the dominant cost was the READ path, which round-21 never touched. getShippingData() (the single read entry behind server.loadShippingData, called by 59 routes) did getAppState() — a full ~1.6MB blob pull from Postgres — on EVERY request, with no cache. The same external client still POSTing /admin/:moduleKey/exchange-rates/refresh ~every 2s hit loadShippingData(force) which ALWAYS does the full read at line 833 BEFORE the throttle gate; the throttle blocked the write but not the read. Live pg_stat_statements sampling proved it: 38–39 reads/min ≈ ~55k reads/day × ~1.6MB ≈ ~70GB/day, still active, writes at 0. Fix (structural, trigger-agnostic): (A) in-process read cache in getShippingData — serve deep clones from a cached normalized blob; reads collapse to cache hits (zero DB egress); call-time TTL (default 15min, env SHIPPING_CACHE_TTL_MS) bounds the egress floor to ~1 pull/window (~96/day) and bounds cross-instance staleness; (B) saveShippingData now diffs against the cache and writes only the one changed section via jsonb_set ({modules,<key>}), skips no-op writes entirely, pins exchangeRates to the freshest value so a module save can't roll back a concurrent FX update (symmetric to round-21's FX-only jsonb_set), and falls back to a full overwrite only for cross-module changes; (C) every write path refreshes the cache (write-through) so the operator sees their own change immediately. Single-instance deploy makes write-through authoritative (TTL is pure egress/staleness safety net). Thick test (audit-rmw-cache-test, mocks the db layer): 100 reads => 0 DB pulls, read isolation, targeted vs full write, no-change-no-write, FX slice refresh, FX pin, TTL expiry — 9/9; full suite 10/10.
- lesson: (a) for a managed store with per-byte egress, the READ path can dwarf the write path — a write-storm fix that ignores an uncached full-blob read leaves the egress quota cliff intact; always profile reads (pg_stat_statements SELECT calls × payload size), not just writes (revision delta). (b) revision/write metrics are blind to read egress — measure read-call rate separately. (c) a per-request full-blob read behind many routes is the read-side dual of the full-blob write storm; the fix is the same shape — don't move the whole object when you can cache it / write one field. (d) put the cache at the single read entry point, serve clones (callers mutate), make it write-through (operator sees own edits) with a TTL safety net sized so the egress floor (≈ 1 pull per TTL under a relentless caller) stays under quota; read TTL at call time so it's tunable without a deploy. (e) a fix can be "deployed and verified" on its stated metric (writes=0) yet the real cost (egress) keeps bleeding — re-derive the actual cost driver from first principles, don't trust the earlier framing.
- scope: project (a/b/c/d are strong cross-project candidates → LESSON_CANDIDATES)
- landed_in: src/lib/store.js (read cache + targeted/no-op/pin write path + write-through), src/lib/db.js (patchAppStateField nested-path support), scripts/audit-rmw-cache-test.js (thick DB-mode test), scripts/rmw-egress-probe.js (read-only before/after egress probe)
- next_action: after deploy, re-run scripts/rmw-egress-probe.js to confirm reads/min ~0 and est. egress collapses; STOP the external /exchange-rates/refresh poller at the source (Railway access log / hung admin tab) for the complete near-zero fix; consider moving exchangeRates to its own small app_state key so FX never rides the big blob; add the round-21 app-layer usage guard (in-memory counter → degrade + alert) as defense-in-depth.

## 2026-06-21 - TTL is the egress knob + add your own real-time usage alarm (don't wait for the month-end bill)

- source_type: project + cross-project candidate
- task_type: cache TTL tuning + application-layer usage guard (alarm + auto-degrade) + read-side hardening
- domain: shipping-data app_state read/write, in-process cache, managed-storage cost safety, observability
- trigger: follow-up to the RMW read-cache killshot (PR #16, merged) — Chandler asked (a) what the 15min TTL means / can it be longer, and (b) why we only find out about a storm at the month-end bill instead of when it happens.
- incident_or_feedback: two refinements on top of the read cache. (1) TTL = the egress floor: under a relentless caller the cache misses exactly once per TTL window, so egress ≈ (1 read / TTL) × blob. The DB read frequency and the FX *fetch* frequency are SEPARATE concerns — FX only needs fetching ~once/day (the scheduler); the cache TTL only governs how often the 1.6MB blob is re-pulled from Postgres. On a single-instance deploy with write-through, a longer TTL has zero staleness cost in normal operation (own writes update the cache immediately), so we raised the default 15min → 1h (~24 pulls/day ≈ ~1.1GB/month, well under the 5GB free tier) and kept it env-tunable (SHIPPING_CACHE_TTL_MS, read at call time). The one cost of a long TTL: out-of-band writers (scripts/patch-prod-data.js, db:seed run in a separate process) don't update the live server's cache, so after a prod patch you must redeploy (restart clears the cache) or wait one TTL before spot-checking — a real deployment discipline. (2) A managed store's free tier has no real-time, fine-grained usage alert, so we added our own app-layer usage guard (src/lib/usage-guard.js, pure in-memory, no DB writes): it counts DB-PENETRATION reads (cache hits never reach db.getAppState, so they're excluded) and writes per day, screams a loud deduped [USAGE-GUARD-ALERT] in the Railway logs when a daily threshold is crossed (default read 200 / write 500), and auto-degrades the runaway behavior — over the write threshold it DROPS FX (auto) writes while still serving the cached rate, but always lets USER writes (admin editing rates / creating carriers) through; a severe read storm forces a longer cache-TTL floor to clamp egress. Counters reset daily; status is exposed at GET /healthz and logged at startup.
- lesson: (a) for a per-byte-billed cache in front of a managed store, TTL is the egress dial — size it from "(seconds/window ÷ TTL) × payload vs quota", and don't conflate the cache-refresh cadence with the upstream-fetch cadence (they're independent). (b) On single-instance + write-through, a long TTL is free (no staleness for the operator); the only price is out-of-band writers needing a restart/wait — make that a written deployment rule. (c) Don't rely on the provider's billing/alerts to catch a cost storm — add a cheap in-memory usage guard that counts the actual expensive op, alerts loudly+deduped at an abnormal multiple of normal volume, and degrades the RUNAWAY behavior (auto/machine writes), never the user's actions, never the whole service. (d) Count the real cost driver: DB-penetration reads (exclude cache hits) and writes — and make the guard itself free (no DB writes to count). (e) Distinguish auto (degradable) vs user (must-pass) writes when degrading.
- scope: project (a/c/d/e are strong cross-project candidates → LESSON_CANDIDATES, complements the round-24 read-side entry)
- landed_in: src/lib/usage-guard.js (new), src/lib/db.js (count at getAppState/saveAppState/patchAppStateField), src/lib/store.js (TTL default 1h + guard-aware TTL floor + FX auto-write degrade), src/server.js (startup log + GET /healthz), scripts/audit-usage-guard-test.js (8/8)
- next_action: after deploy, watch /healthz + Railway startup log; confirm reads ~24/day under 1h TTL; remember the redeploy-after-patch rule; optional step-2 alerting (email/webhook) needs a service + recipient from Chandler; still stop the external poller at source (F1).

## 2026-06-21 - Can't stop an external caller from the server — identify it in-app, neutralize the work, stop at origin

- source_type: project + cross-project candidate
- task_type: locate + neutralize an external high-frequency caller (F1) on a route the app does not itself call
- domain: forced FX-refresh route, request-source observability, route rate-limit
- trigger: after the egress storm was killed (read cache) the external client STILL POSTed /admin/:moduleKey/exchange-rates/refresh ~every 2s — DB/egress harmless (throttle + cache) but the source was unknown and the HTTP noise remained.
- incident_or_feedback: confirmed first that our own frontend does NOT poll the route (grep of public/ + views/: the only caller is a manual button form; the AJAX submit handler is scoped to calculator submits; no setInterval/fetch loop) — so the trigger is external to the codebase. Railway's access log would name the source, but the Railway CLI's OAuth token was expired and `railway login` is an interactive browser action (user-only), so I could not read it. Solution: capture the source IN-APP. Added src/lib/refresh-monitor.js (pure in-memory) that records each refresh hit's NON-secret fingerprint (client IP from X-Forwarded-For + socket, User-Agent, Referer, timestamp — never cookies/auth), aggregates distinct sources, and exposes them at GET /healthz.refreshRoute — an endpoint I CAN curl without Railway access. Also added a route min-interval gate (default 5s, env): a hammered route records the source then short-circuits the (already cheap) refresh and just redirects; the manual button and the daily scheduler are unaffected. End-to-end verified: 3 rapid POSTs -> 1 refreshed + 2 skipped, source fingerprint captured.
- lesson: (a) you cannot make an external client stop sending requests from the server side — the achievable goals are: prove it's not your own code (grep every caller), make handling it free (throttle + cache already did), IDENTIFY it (capture the non-secret request fingerprint), and hand the fingerprint to whoever can stop it at origin. (b) When the platform's logs are unreachable (expired/again-needed interactive auth you can't perform), build the diagnostic INTO the app and expose it on an endpoint you can already reach (/healthz) — don't block on access you don't have. (c) Capture only non-secret metadata (IP/UA/Referer), never cookies/auth/session. (d) A route min-interval gate is the "rate-limit the expensive op at its trigger" pattern applied at the HTTP layer — safe when the legitimate caller (a human clicking, a daily cron) operates far below the interval. (e) Distinguish "neutralized" (no longer harmful) from "stopped" (no longer happening) — be explicit in the report which one you achieved; here the storm is neutralized + the source is now identifiable, but stopping it at origin needs the owner.
- scope: project (a/b/c/d are cross-project candidates → LESSON_CANDIDATES)
- landed_in: src/lib/refresh-monitor.js (new), src/server.js (refresh route capture + min-interval gate, /healthz.refreshRoute), scripts/audit-refresh-monitor-test.js (5/5)
- next_action: after deploy, curl prod /healthz.refreshRoute.sources to read the poller's IP/UA/Referer; if the fingerprint points at a page, fix that page; if external, give the fingerprint to Chandler/José to stop at origin (close the tab / kill the monitor) and optionally add a targeted block; once stopped, the /healthz source detail can be tightened or removed.
- outcome (2026-06-21): deployed + observed ~5 min → 0 refresh-route hits, so the poller had ALREADY stopped on its own (PR#16's cache only makes hits cheap; it can't stop the HTTP requests, so 0 hits = the client itself is gone). Could not fingerprint it live (inactive). The monitor stays deployed as a permanent trap: if it (or any future high-frequency caller) returns, /healthz.refreshRoute captures the source instantly and the route min-interval gate caps the work. Reinforces lesson (e): we achieved "neutralized + trap-armed"; the source self-stopped before we could fingerprint it live.

## 2026-06-22 - God-file refactor: test-net FIRST, pure-move via ctx, verified splice; JSON-mode is faithful for code moves

- source_type: project + cross-project candidate
- task_type: structural refactor of two god-files (server.js 4707 / store.js 2606) — behavior-exact, phased
- domain: code organization (Express routes/middleware/lib), test harness
- trigger: split server.js/store.js into modules with documented one-way boundaries, "report diff = 0".
- incident_or_feedback: executed Phases 0-2 (test:all runner + admin-route coverage net; middleware/ + small routes; workbench routes). Key decisions: (1) Local Postgres was unavailable (no Docker) AND unnecessary — the codebase's own tests run STORAGE_DRIVER=json in-process (createApp + HTTP), which faithfully verifies a pure code-move (the storage driver is orthogonal to where code lives) with zero prod risk; chose that over standing up a DB that the tests would ignore. (2) Built the test net BEFORE touching server.js (no green baseline → no refactor) — added a route-level admin-CRUD test (isolated temp DATA_DIR) filling ~47 untested routes, so any extraction mistake surfaces as a red test. (3) Pure-move pattern: routes → routes/<module>.js exporting register(app, ctx); lib functions imported directly, server.js helpers passed via a single ctx object assembled once in createApp (transitive helper calls resolve via each helper's own closure, so ctx only needs the directly-referenced ones). (4) For large block removals, a node splice script that ASSERTS the boundary lines (first/last of each range) before writing — safer than hand-transcribing 200+ lines into an Edit. (5) Verified each phase by: route-count conserved, middleware order preserved, load OK, test:all green, quote-test (exact outputs) as the behavior-diff. Stopped after Phase 2 and deferred Phase 3 (admin, ~60 routes + closures + helper downshift) / Phase 4 (store split) to a focused pass rather than rush the riskiest parts at end-of-turn on a production system.
- lesson: (a) for a behavior-exact refactor, the FIRST deliverable is the test net, not the refactor — a thick route-level suite turns risky moves into safe ones (red test catches any miss). (b) The storage driver is orthogonal to a pure code-move; don't block on prod-fidelity infra you can't stand up — use the codebase's native test mode. (c) ctx-passing (one object built in the composition root) cleanly decouples extracted routes from still-in-place helpers without circular deps or threading every transitive dep. (d) Splice large removals with a script that asserts boundaries; verify with invariants (route count, middleware order, load, exact-output tests) not eyeballing. (e) Phase the riskiest parts (god-file admin section, closures, thin-coverage routes) into their own pass — incremental green commits beat a rushed big-bang on production.
- scope: project (a/b/c/d/e are cross-project refactor candidates → LESSON_CANDIDATES)
- landed_in: scripts/run-all-tests.js, scripts/audit-admin-routes-test.js, src/middleware/{auth,i18n,locals}.js, src/routes/{health,exchange-rates,workbench}.js, src/server.js (4707→~4150), package.json (test:all)
- next_action: Phase 3 (admin routes per-module + lib/ helper downshift, one module per commit) then Phase 4 (store.js → lib/store/*, API unchanged), each test:all-green; finalize ARCHITECTURE module-boundaries section when all phases land.


## 2026-06-22 - God-file refactor Phase 3-5: full decomposition (server.js 4707->107, store.js -> lib/store/*)

- incident_or_feedback: completed the refactor end-to-end in one focused pass (no defer): Phase 3a thickened the admin test net (audit-admin-deep-test.js, 16 sub-tests with a hermetic OSRM fetch mock so route-cache writes are deterministic with zero network), then Phase 3b sank the inline helpers to lib/ (rule-engine, customs-rules, handover-forms) and extracted the admin routes per module (admin-inland/customs/shipping-lines/handover/settings) + the view layer (lib/views.js, 38 helpers) + core routes, then Phase 4 split store.js, then Phase 5 docs. server.js 4707->107 lines; store.js -> 7-file directory. 13 commits, one per module, each verified green.
- lesson: (a) PURE-MOVE MECHANICS: for scattered/large moves, a boundary-asserting node splice (find a decl by name, scan to its col-0 closing brace, remove it + leading // comments + one trailing blank) beats hand-Edits; for whole-block route extraction, slice the block verbatim into register(app, ctx) and auto-derive the export/import name list from it. Always import the moved symbols back into the source, then PRUNE the now-unused imports (count occurrences; total of 1 == import-only == dead). (b) DEPENDENCY-DIRECTED SPLITTING: before splitting an interdependent module, build the call graph and assign by acyclic layers; for store.js a generator asserted layer rank (shared < per-entity normalizers < composer < index) and FAILED on any same/upward cross-ref, naming exactly which helper to move down to the shared leaf (caught 6 cross-shared rule-list normalizers + comment false-positives, fixed by stripping comments before ref-detection). (c) FILE->DIR RELOCATION FOOTGUN [SELF_CORRECTION]: moving lib/store.js to lib/store/index.js puts it one directory deeper, so every __dirname-relative path shifts a level. bundledDataDir = join(__dirname, "../../data") silently became src/data, so the JSON seed read fell back to EMPTY (handover.shippingLines became 0). The test net caught it (admin-routes went red); fix was "../../../data". Also: module-level let vars and bare top-level statements (loadLocalEnv()) are not function/const declarations, so a name-based extractor drops them — re-add explicitly. And require("../lib/store.js") with an explicit .js extension does NOT resolve to a directory's index.js — drop the extension. (d) TOOLING: BSD/macOS grep treats UTF-8 files containing CJK as binary and SILENTLY skips them (store.js had Chinese strings) — use grep -a for any file with non-ASCII. (e) VERIFY: per-commit invariants = route-count conserved (67), middleware order, load OK, test:all green, quote-test exact-output diff=0; plus a final adversarial multi-reviewer pass diffing every new module against the pre-refactor original (d8e0c28) confirmed 0 behavior changes. (f) ctx END-STATE: the transitional per-module ctx collapses into ONE shared container (the views module + the few non-view helpers) once helpers live in lib — passing it to every register() keeps modules decoupled from createApp without rewriting them.
- scope: project (a-f are cross-project refactor candidates -> LESSON_CANDIDATES; c+d are high-value engineering gotchas)
- landed_in: src/lib/{rule-engine,customs-rules,handover-forms,views}.js (new), src/routes/{core,admin-inland,admin-customs,admin-shipping-lines,admin-handover,admin-settings}.js (new), src/lib/store/{shared,normalize-handover,normalize-customs,normalize-inland,normalize-quote,normalize-shipping-data,index}.js (store.js split), src/server.js (4707->107), scripts/audit-admin-deep-test.js (new, 16 tests), docs/ARCHITECTURE.md (module boundaries). PR #22.
- outcome: test:all 14/14 + quote-test 9/9 throughout; route count 67 + route-path literal set byte-identical to d8e0c28; adversarial review 0 confirmed behavior changes. Honest: code-structure only, zero behavior/DB/prod change; decoupling does NOT lower egress (data-layer concern); blob->relational migration is the next independent task.

## 2026-06-22 - "File doesn't exist" must mean `git log --all -- <path>`, not a working-tree grep [SELF_CORRECTION]

- source_type: project + cross-project candidate
- task_type: blob→relational migration prep — locate the authoritative schema-design doc both CODEX prompts reference (docs/specs/20260621_blob_to_relational_redesign.md)
- domain: git archaeology, spec recovery, test/prod isolation
- trigger: the prompt's required-reading doc was absent from the working tree on feature/refactor-godfiles.
- incident_or_feedback: I first reported the doc "does not exist, confirmed repo-wide" based on a working-tree filename grep + `git log --all --oneline | grep` of subjects — but NOT `git log --all -- <path>`. The user pointed at commit 90bc784; `git show 90bc784:<path>` returned a full 359-line design doc. `git merge-base --is-ancestor 90bc784 HEAD` = NO: the doc was authored on a DIVERGENT line ("design blob→relational redesign") that the refactor branch never descended from, so it was real in history but absent from the tree. Recovered it (`git show 90bc784:path > path`, commit 1), then reconciled it to the post-refactor src/lib/store/normalize-* shapes (v2, commit 2): fixed 4 fidelity gaps (inland_route_cache key/encoded_polyline; customs-charge vs handover-localCharge shapes; terminal storageRuleSets + two assignment matrices; full inland_rate_entries columns), added a per-deep-structure split-table-vs-JSONB decision table, reverse migration, and STORAGE_MODE=blob|dual|relational + 2a/2b. Separately [INCIDENTAL_FIX]: scripts/run-all-tests.js now forces STORAGE_DRIVER=json in each spawned suite's env, because src/lib/env.loadLocalEnv loads the prod DATABASE_URL from .env and db.shouldUseDatabase() is true whenever it is set + STORAGE_DRIVER is unset — a latent footgun (today no suite both uses the real store AND calls a DB function; audit-contento only calls the pure normalizer; audit-rmw-cache/audit-usage-guard mock shouldUseDatabase). Verified 14/14 green before and after.
- lesson: (a) before claiming a referenced file "never existed", run `git log --all -- <path>` and `git show <commit>:<path>` — a working-tree grep only proves it's not in the CURRENT tree; a doc can be real but stranded on a divergent/unmerged line (check `git merge-base --is-ancestor`). "repo-wide" must include history. (b) When recovering a design doc onto a changed code baseline, reconcile it to the current ground-truth (the normalizers), don't trust the doc's older shapes — and keep recovery (verbatim restore) and revision as SEPARATE commits for provenance. (c) A test harness that loads a prod DB URL from .env is a latent footgun: force the non-prod driver (STORAGE_DRIVER=json) in the runner's child env so no suite can ever reach prod, independent of which suite is added later. (d) Hard-stop honesty: with no non-prod DB and a plan-only schema, deliver the reviewable artifact (the doc) + the safe fix, and STOP before any DDL — don't improvise a schema against irreversible prod data.
- scope: project (a/c/d are cross-project candidates → LESSON_CANDIDATES); relates to the grep -a CJK gotcha already recorded.
- landed_in: docs/specs/20260621_blob_to_relational_redesign.md (restored cfb4c51 + v2 ec7c7f6), scripts/run-all-tests.js (99411d5)
- next_action: schema doc is plan-only — awaits Chandler/Claude review (§G open questions) + a throwaway non-prod DATABASE_URL before any build/migration; production cutover stays a hard-stop per §5 of the prompt.

## 2026-06-23 - Pre-cutover dry-run with REAL prod data caught a text-column data-loss bug that seed + in-memory both missed

- source_type: project + cross-project candidate
- task_type: blob→relational migration — replay real production data through the toolchain in the isolated sandbox before the irreversible cutover
- domain: data migration safety, schema type fidelity, dry-run methodology
- trigger: runbook Step 3-4 gates (Q4 orphan / Q5 currency / parity) "can only run at cutover" on real data — pulled forward into a sandbox dry-run.
- incident_or_feedback: ONE read-only read of prod expressline.app_state (ref==prod assert + `set transaction read only` + only that select; joyas_*/punas_* never touched), loaded into sandbox fnczokogchlhutyskbdw, ran the full toolchain. Real data (21 carriers/28 yards/44 dests) surfaced TWO things the 14-carrier seed never did: (1) [DATA] 8 dangling carrier→yard links — 4 carriers' customs-mirror yardIds point at 3 removed demo yards (yard-mzo-norte/sur, yard-lc-central, replaced by CONTENTO); the demo→CONTENTO swap happened in the normalizer, not via the yard-delete route, so inbound refs were never cleaned. Q4 caught it; migrate-forward aborted per the gate rule (not bypassed). (2) [BUG, mine] customs_note was modeled `text`, but 8 carriers' mirror notes is an OBJECT ({sourceSheet,code,rfc} copied at mirror creation), so the text column stringified it to "[object Object]" = silent data loss. CRITICAL: the in-memory round-trip (normalize(assemble(decompose(blob)))) MISSED this — it has no column-type coercion — and the seed missed it (string notes). ONLY the real DB round-trip on real data exposed it (parity showed 20 diffs: 12 yardIds + 8 `notes [object vs string]`). Fixed customs_note text→jsonb; re-ran → parity dropped to exactly the 12 yardIds. Then a sandbox-only "reconcile preview" (drop the dead links, lossless) → both gates PASS, full toolchain clean.
- lesson: (a) a pre-cutover dry-run that replays REAL production data through the migration in an isolated sandbox (one read-only prod read) is mandatory before an irreversible data migration — seed/synthetic data and in-memory round-trips do NOT exercise column-type coercion or real-data edge shapes. (b) An in-memory decompose/assemble round-trip is necessary but NOT sufficient: it can't catch text-vs-jsonb coercion, numeric(p,s) rounding, date/timestamptz→string, etc. Run parity THROUGH the real DB on REAL data. (c) Model any field whose value can be a string OR object (a denormalized note that sometimes copied a structured object) as jsonb, never text. (d) Replacing seed entities inside the normalizer (demo yards → CONTENTO) does not clean inbound references elsewhere (carrier yardIds) — leaves dangling refs invisible until real data hits the FK/orphan gate. (e) Keep the gate a hard-abort for the real cutover, but add a SANDBOX-ONLY --allow-gate-fail dry-run switch so a diagnostic can characterize full impact past the first gate without ever weakening the prod path; and a clearly-labeled sandbox "reconcile preview" proves the fix before asking for approval. (f) "repo-wide / fully verified" must include the real-data DB round-trip, not just code + seed tests.
- scope: project (a-f are cross-project migration candidates → LESSON_CANDIDATES)
- landed_in: scripts/relational/{read-prod-blob,load-snapshot-to-sandbox,reconcile-orphans-to-sandbox,prod-dryrun-facade}.js (new), migrate-forward.js (--allow-gate-fail), parity.js (full diff shapes), src/lib/store/relational-repo.js + relational-map.js (customs_note text→jsonb), docs/specs/20260621_blob_to_relational_redesign.md
- next_action: await Chandler reconcile decision on the 8 dead links (recommend lossless drop, or remap to CONTENTO yards if José intended a real mapping); customs_note→jsonb fix is independent and ready. .prod-blob-snapshot.json is real prod data — gitignored, delete after; sandbox holds the prod snapshot — delete after cutover.

## 2026-06-23 - DROP reconcile of the 8 dangling carrier→yard refs: re-validated on the real prod snapshot; restore-by-upsert leftover [SELF_CORRECTION]

- source_type: project + cross-project candidate
- task_type: blob→relational migration — execute Chandler's DROP decision (the 8 dead carrier→yard links target removed demo yards = lossless) as a PERMANENT migration normalization, then re-validate on the real prod snapshot in sandbox fnczokogchlhutyskbdw (zero prod writes). Implementation landed in commit 0cb1277; this pass independently reproduced every claim.
- domain: data migration safety, idempotent-upsert semantics, sandbox state hygiene
- trigger: the prior dry-run (2026-06-23) surfaced 8 dangling refs and recommended DROP-or-remap; Chandler chose DROP.
- incident_or_feedback: gates.dropDanglingRefs drops ONLY carrier↔yard refs whose target id ∉ the live id set (deleted = lossless), logs each ref, and migrate-forward runs Q5(raw) → normalize+drop → Q4(post-drop) so any OTHER orphan class (target exists, mis-bucketed) STILL aborts — never silently swallowed. Re-validated two independent ways: (a) OFFLINE from .prod-blob-snapshot.json (no DB) recomputed the exact 8 drops — cma-cgm×2 / maersk×3 / zim×2 / msc×1 → the 3 removed demo yards yard-mzo-norte / yard-mzo-sur / yard-lc-central — with Q4 pre=FAIL(8) and post=PASS(0); (b) on the sandbox DB: Q5 PASS → 8 logged → Q4 post PASS, parity DATA diff=0, reverse(forward(blob))==normalize(blob) YES, forward IDEMPOTENT (2nd run identical counts + parity still 0), prod-dryrun blob(post-drop)==relational, integration-test 9/9, test:all 14/14. José hand-edits intact: CMA doc fee=50 (cma-cgm-3), KMTC ISD=15 (kmtc-5 group rates 15 USD), ZIM present, COSCO 3 charges, 2 self-built yards (新场站 4/5), 7 empty-shell carriers (esl-emirates/sinokor/sl/sea-lead/ts-lines/hmm/sinotrans). Post-drop the 4 affected carriers are yard-less — but so are ALL 21 (0/21 carriers, 0/28 yards mapped = método B; CONTENTO yards are cost-side inert until José maps them) → NOT anomalous. [SELF_CORRECTION]: my sandbox-restore step ran load-snapshot + migrate-forward AFTER integration-test had reset the schema and seeded DEMO data; upsertAllTables is insert/update-only (no delete-not-in-set), so 2 demo rows whose ids ∉ prod survived (carrier_local_charges 46→47, inland_route_cache 44→45) and parity then FAILED (localCharges len 6 vs 7 / routeCache len 44 vs 45). Fix: migrate-schema --reset (drop+recreate empty tables) → reload snapshot → migrate-forward → parity back to 0. Sandbox left clean & in the validated state (app_state = raw prod snapshot/rollback source; tables = post-drop, parity=0).
- lesson: (a) [SELF_CORRECTION] re-pointing sandbox entity tables to a DIFFERENT dataset than what currently populates them requires a schema --reset FIRST (or a delete-not-in-set), because the migration's upsert never removes rows absent from the new set — orphan rows from the prior dataset survive and silently break parity. After ANY test that reseeds/reschemas the sandbox (integration-test does both), reset before reloading a different dataset. (b) An offline recompute straight from the snapshot file (no DB) is a cheap, strong independent cross-check of a DB migration's drop/gate claims — reproduce the exact dropped-ref list + pre/post gate state before trusting the DB run. (c) "entity becomes yard-less after a drop" is only anomalous RELATIVE to peers — when ALL carriers are yard-less (método B), a yard-less result is the baseline, not a regression; compare affected entities against the GLOBAL mapping rate before flagging. (d) The Step-5 dual-shadow equal=false is EXPECTED, not drift: the blob retains the dangling refs as the rollback source while the tables are cleaned, so the only delta must be exactly these 8 known drops — the cutover monitor applies the same drop to the blob side or whitelists exactly these 8; any OTHER delta is real drift → stop.
- scope: project (a/b/c are cross-project migration candidates → LESSON_CANDIDATES)
- landed_in: scripts/relational/{gates,migrate-forward,migrate-reverse,parity,prod-dryrun-facade}.js + docs/specs/20260622_blob_to_relational_CUTOVER_RUNBOOK.md (commit 0cb1277, feature/refactor-godfiles). This pass changed no code — re-validation only + this lesson.
- next_action: production cutover stays a HARD-STOP (this was sandbox validation only). .prod-blob-snapshot.json stays gitignored + the sandbox is retained for validation; delete both after cutover. At cutover Step 5, confirm the only dual-shadow delta is the 8 known drops.

## 2026-06-24 - Prod DB build+populate (Steps 1–4) on a SHARED 3-business project: restricted-role isolation proof is the safety foundation

- source_type: project + cross-project candidate
- task_type: blob→relational PROD cutover Phases 0–4 (backup → restricted role + isolation proof → 18 tables → forward migrate + 8-drop + gates → parity) on prod `polxyashvxbzdkkmxuox`, which hosts THREE live businesses (expressline + joyas + punas) in ONE Supabase project. DB-only: build + populate + verify the relational tables WITHOUT touching the running app (no merge, no deploy, no STORAGE_MODE change). App keeps reading the blob, zero downtime.
- domain: production data migration, multi-tenant schema isolation, least-privilege DB roles, non-destructive cutover
- trigger: sandbox dry-run + DROP reconcile done (parity=0); the prompt authorized a continuous Phases 0–4 prod run, stop only on a hard-gate failure.
- incident_or_feedback: built a prod harness (`scripts/relational/prod-{guard,env,00-backup,01-role-and-isolation,02-create-tables,03-migrate-forward,04-parity-reverse,99-final-verify}.js`) that is a thin wrapper of the SAME sandbox-verified modules (relational-repo/relational-map/gates/normalize-*) — only the connection target + guard differ, so what was parity-verified on the sandbox is byte-identical code on prod. All 5 hard gates passed: Phase 0 backup (app_state 2 rows sha 10a690a2…, audit_logs 0, quote_snapshots 5; gitignored `backups/`); Phase 1 `expressline_migrator` role + isolation proof (migrator SELECT on real joyas_asset_*/punas_* = ALL permission-denied 42501, positive control on expressline passed, connects via Supavisor pooler as `<role>.<ref>`); Phase 2 18 tables created, migrator-owned, app_state untouched; Phase 3 pinned the live blob (sha 0b7acb37…) then Q5 PASS → 8 dangling drops (cma-cgm×2/maersk×3/zim×2/msc×1 → yard-mzo-norte/sur, yard-lc-central) → Q4 post PASS(0) → upsert (app_state NOT written); Phase 4 parity=0 (×2, incl. post-idempotent re-upsert), reverse==normalize YES (verify-only), José edits intact (CMA doc 50 / KMTC ISD 15 / ZIM / COSCO / 2 self-built yards / 7 empty-shell carriers). Two deliberate hardenings OVER the runbook (both stricter): (i) granted the migrator only SELECT (not `all`) on the existing app_state/audit_logs/quote_snapshots, so it physically cannot modify app_state — iron rule "don't touch app_state" enforced at the privilege layer; (ii) reverse is verify-only (no rollback-key write to prod app_state). Live-data note: `carrier_local_charges`=45 on prod vs 46 in the snapshot dry-run — the live blob is newer than `.prod-blob-snapshot.json` (José edited one charge since), so Phase 3 PINS the live blob and Phase 4 parities against the pin (deterministic despite the active FX-storm writer, rev 215124).
- lesson: (a) on a SHARED multi-business Supabase project, project-ref assertion is necessary but NOT sufficient — the real isolation is a least-privilege ROLE proven EMPIRICALLY: create `<schema>_migrator` with privileges on the owned schema only, then PROVE it by SELECT-ing real other-tenant tables and requiring permission-denied (42501) before any write. Enumerate the REAL other-tenant table names (the runbook guessed `joyas_products`; actual is `joyas_asset_*`) — a guessed name that doesn't exist yields 42P01, an ambiguous non-proof. (b) grant the migrator SELECT-only on pre-existing tables and USAGE+CREATE on the schema so it OWNS (and can only fully write) the NEW tables — a code bug then cannot mutate the live blob; defense-in-depth beats "my script won't write it". (c) when the source blob has a LIVE high-frequency writer, PIN it (read once to a local file + sha) and run forward+parity+reverse all against the pin — otherwise a concurrent write makes parity a false-fail. (d) a prod cutover wrapper should change ONLY the connection target+guard vs the sandbox-verified modules, so parity=0 on the sandbox transfers; never fork the DDL/IO/gate logic for prod. (e) keep each phase a hard gate with a --execute flag (preview computes/connects read-only, execute writes in a txn) so the first prod write is deliberate; Supabase `postgres` (pooler user) has CREATEROLE and is sufficient for the one-time role setup; custom login roles DO connect through the Supavisor pooler as `<role>.<ref>`.
- scope: project (a–e are cross-project production-migration candidates → LESSON_CANDIDATES)
- landed_in: scripts/relational/prod-*.js (new harness), docs/specs/20260622_blob_to_relational_CUTOVER_RUNBOOK.md (execution record). Backups/pin/creds gitignored (backups/, .env.prod-migrator). No business code, no app deploy.
- next_action: STOP for Claude review. Steps 5–8 (deploy STORAGE_MODE=dual→relational→retire blob) remain and need the PR merge + 2b route swap first. Prod now has 18 built+populated parity=0 tables; app still on blob. For the deploy, resolve table OWNERSHIP (the app role `postgres` needs grants on the migrator-owned tables, or transfer ownership). Keep backups/.prod-migration-pin/.env.prod-migrator local. At Step 5 confirm the only dual-shadow delta is the 8 known drops.
